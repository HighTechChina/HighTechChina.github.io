# 博雅高科 · 部署说明

本项目是一个 Hugo 网站（Ananke 主题），以「博雅小筑」为模板改造，面向中国高科技内容展示。
首页大图与博雅小筑同一张（`static/images/background.jpg`）。

- 站点标题：博雅高科 | HighTechChina
- 首页副标题：中华科技·博览天下
- 联系邮箱：hightech@agent.qq.com
- 目标地址：**https://hightechchina.github.io/**
- GitHub 账号：**HighTechChina**
- 本机构建：项目已随附 `tools/hugo/hugo.exe`，`deploy.bat` 会优先使用它（无需另装 Hugo）

---

## 一、准备工作（只做一次）

### 1. 用 HighTechChina 账号新建仓库
- 登录 GitHub（HighTechChina 账号）→ 右上角 **+** → **New repository**
- 仓库名必须**严格**等于：`HighTechChina.github.io`
- 设为 **Public**，不要勾选 Add README，点 **Create repository**

### 2. 准备一个「个人访问令牌 PAT」（代替密码，安全可撤销）
> GitHub 从 2021 年起不再支持账号密码推送，必须用 PAT。
- GitHub 头像 → **Settings** → 最底部 **Developer settings**
- **Personal access tokens** → **Tokens (classic)** → **Generate new token (classic)**
- 勾选 **repo** 和 **workflow** 两个权限，生成后**复制保存**（只显示一次）
- 之后 `git push` 提示输密码时，把这个 PAT 粘进去即可

---

## 二、上线（二选一）

### 方式 A（推荐 · 最省事 · 不依赖本地 Hugo）
用 GitHub Actions 在云端自动构建，你只需把文件推上去。

1. 把整个 `HighTechChina` 文件夹（含 `.github/workflows/hugo.yml`）传到那台电脑
2. 双击运行 **`deploy.bat`**
   - 首次会自动 `git init` 并绑定远程仓库
   - 输入更新说明后回车
   - 弹出登录时：用户名填 `HighTechChina`，密码粘贴上面的 **PAT**
3. 打开仓库 **Settings → Pages → Build and deployment → Source** 选 **GitHub Actions**
4. 回到仓库 **Actions** 标签，等绿勾跑完（约 1-2 分钟）
5. 访问 **https://hightechchina.github.io/** 🎉

> 之后每次改完内容，只要再双击一次 `deploy.bat` 就会自动重新部署。

### 方式 B（你现有的本地构建习惯）
如果你想沿用博雅小筑那台机器上「本地 hugo 构建再推」的流程：

1. 在项目根目录执行：`hugo --minify`（生成 `public/`）
2. 把 `public/` 里的内容推到 `HighTechChina.github.io` 仓库
3. Settings → Pages → Source 选 **Deploy from a branch**（main / root）

---

## 三、日常怎么写文章

- 新文章放到 `content/posts/` 下，新建 `xxx.md`，开头照抄示例文章的 front matter
- 想配图：图片放 `static/images/`，front matter 里写 `featured_image: "/images/xxx.jpg"`
- 栏目已建好：前沿资讯 / 大国重器 / 硬核拆解 / 产业观察（对应 `content/` 下同名文件夹）
- 想换某个栏目/关于页的说明，改对应文件夹里的 `_index.md` 或 `about.md`

## 四、目录结构一览

```
HighTechChina/
├─ hugo.toml              站点配置（标题/副标题/菜单/社交）
├─ deploy.bat            一键上传脚本
├─ 部署说明.md            本文件
├─ .github/workflows/    GitHub Actions 自动构建
├─ content/
│  ├─ _index.md          首页双语欢迎语
│  ├─ about.md           关于页
│  ├─ posts/             前沿资讯（含示例文章）
│  ├─ daguo/             大国重器
│  ├─ hardcore/          硬核拆解
│  └─ industry/          产业观察
├─ static/
│  ├─ images/background.jpg   首页大图（同博雅小筑）
│  └─ css/custom.css          双语板块/绿色强调字样式
└─ themes/ananke/        主题
```
